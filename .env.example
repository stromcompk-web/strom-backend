PORT=3001
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# Admin login — set in .env or server env only; never commit. Seed creates this user in DB on first run.
ADMIN_EMAIL=your-admin@example.com
ADMIN_PASSWORD=set-a-secure-password
CORS_ORIGIN=http://localhost:5173,http://localhost:8080
DATABASE_PATH=./data/strom.db
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# DATABASE_URL=mongodb+srv://user:pass@cluster.mongodb.net/strom?retryWrites=true&w=majority
# DATABASE_SSL=false
